<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:circle="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             BackgroundColor="{StaticResource AlmostBlack}"
             x:Class="SampleApp.Views.ProfileContentView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <BoxView Grid.Row="0" BackgroundColor="{StaticResource PageFadeColor}" Opacity="0.3"/>

        <circle:CircleImage Grid.Row="0"
                            Grid.RowSpan="2"
                            BorderThickness="2"
                            BorderColor="{StaticResource OrangeColor}"
                            Source="tony-stark.jpg"
                            HeightRequest="100"
                            WidthRequest="100"
                            VerticalOptions="Start"
                            HorizontalOptions="Center"/>

        <Grid Grid.Row="2">
            <Label Text="Profile View"
                   HorizontalOptions="Center"
                   FontSize="30"
                   TextColor="LightGray"/>
        </Grid>
        
    </Grid>
</ContentView>
